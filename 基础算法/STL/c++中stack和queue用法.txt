栈的基本操作：
1.s.empty() 如果栈为空返回true，否则返回false
2.s.size() 返回栈中元素的个数
3.s.pop() 删除栈顶元素但不返回其值
4.s.top() 返回栈顶元素并不删除该元素
5.s.push() 在栈顶压入新的元素

队列的基本操作
1. q.empty() 判空
2. q.size() 返回队列中元素的个数
3. q.pop() 弹出队列首元素
3. q.front() 返回队首元素的值
4. q.push() 压入新元素
5. q.back() 返回队列尾元素